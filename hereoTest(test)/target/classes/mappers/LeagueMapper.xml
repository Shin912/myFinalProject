<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hereo.project.dao.LeagueDAO">
	<select id="selectAllHitRecord" resultType="com.hereo.project.vo.PlayerrecordHitterVO">
		select * from `playerrecord-hitter`
			join `team-player` on ph_tp_num = tp_num
			join `team` on tp_tm_num = tm_num
			join `player` on tp_pl_num = pl_num
			join `members` on pl_me_id = me_id
	</select>
	
</mapper>